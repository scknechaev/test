<div>
    <div class="modal-header">
        <button type="button" class="close" ng-click="cancel()">
            <span aria-hidden="true">×</span>
        </button>
    </div>
    <form ng-submit="choose()" name="chooseForm" novalidate>
        <div class="modal-body">
        <h4>Choose media file</h4>
            <select id="file-select" class="form-control" ng-model="file" name="file" required ng-options="file.name for file in mediaArr"></select>
        </div>
        <div class="modal-footer">
            <button type="button" class="btn btn-default" ng-click="cancel()">Close</button>
            <button type="submit" class="btn btn-primary" ng-disabled="chooseForm.$invalid">Choose</button>
        </div>
    </form>
</div>
